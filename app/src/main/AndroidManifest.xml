<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.velaphi.untamed">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".UntamedAfricaApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".features.animal_details.DetailedDescriptionActivity"/>
        <activity
            android:name=".features.animal_details.AnimalDetailsActivity"
            android:label="@string/default_title"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".features.old_animalDetails.ImageSliderActivity" />
        <activity android:name=".features.old_animalDetails.AllVideosActivity" />
        <activity android:name=".features.old_animalDetails.AllImagesActivity" />
        <activity
            android:name=".features.old_animalDetails.VideoPlayerActivity"
            android:theme="@style/FullscreenTheme" />
        <activity android:name=".features.getInvolved.FoundationDetailsActivity" />
        <activity android:name=".features.safaries.SafariDetailsActivity" />
        <activity
            android:name=".features.old_animalDetails.ImageViewerActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_media_view"
            android:theme="@style/FullscreenTheme" />
        <activity
            android:name=".features.old_animalDetails.AnimalDetailsActivity"
            android:label="@string/title_activity_animal_details"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".features.animalList.AnimalListActivity" /> <!-- <activity -->
        <!-- android:name=".features.main.MainActivity" -->
        <!-- android:label="@string/app_name" -->
        <!-- android:theme="@style/AppTheme.NoActionBar"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->
        <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->
        <activity
            android:name=".features.main.MainBottomNavActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyD0SXbkX4HptbJMQeU-b_hVjJdhoL5c8h8" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>